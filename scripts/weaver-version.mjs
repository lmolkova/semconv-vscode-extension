// The pinned OpenTelemetry Weaver release. Single source of truth for both the
// vendored JSON schema (scripts/sync-schema.mjs) and the registry validation
// (scripts/check-registry.mjs). Bumped by Renovate — see the customManager in
// renovate.json.

// renovate: datasource=github-releases depName=open-telemetry/weaver
export const WEAVER_VERSION = "v0.24.0";
